# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SCJComponet' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'NetWorking', :path => 'Lib/NetWorking'
  pod 'NetWorking', :path => 'Lib/Login'
  # Pods for SCJComponet

end
